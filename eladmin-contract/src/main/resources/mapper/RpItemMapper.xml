<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yntovi.service.mapper.RpItemMapper">

  <!-- 通用查询结果列 -->
  <!--
  <sql id="Base_Column_List">
      a.rpi_id, a.contract_id, a.num, a.no, a.name, a.condition, a.proportion, a.amount, a.estimated_time, a.name_full, a.deleted, a.version, a.create_by, a.create_dept_by, a.update_by, a.create_time, a.update_time
  </sql>
  -->
</mapper>
